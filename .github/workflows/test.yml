name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and dev extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,semantic]"

      - name: Lint with ruff
        run: ruff check .

      - name: Run tests
        # `slow` tests (tests/test_scale.py) are opt-in only — they make
        # timing assertions that flake on shared CI runners.
        # Coverage floor 90% is conservative (current is ~95%); raise as
        # untested-edge work like the CLI stub and live provider path
        # gets covered by future test tracks.
        run: pytest -v -m "not slow" --cov=thriftai --cov-report=term-missing --cov-fail-under=90
