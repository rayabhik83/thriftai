# Quick Start

*Placeholder — install, define agents with `@ta.agent`, run live, replay with `Session.replay(live=[...])`, read `CostReport`. Expanded version of the README example with annotations.*
