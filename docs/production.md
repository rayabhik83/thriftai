# Production Guide

*Placeholder — when to enable, kill switch (`Session(enabled=False)` / `THRIFTAI_DISABLED=1`), known gaps (no TTL, single-instance SQLite, unencrypted at rest), and the per-model break-even table from STRESS_REPORT.md.*
