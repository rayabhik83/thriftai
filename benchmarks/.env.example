# ThriftAI Benchmark Suite — required environment variables.
#
# Copy this file to benchmarks/.env and fill in your key.
# benchmarks/.env is gitignored and never committed.

# Anthropic API key — used for both under-test calls and judge calls.
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=
